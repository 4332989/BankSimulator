using System;
namespace BankSimulator
{
    /// <summary>
    /// putting variable in enum AccountType
    /// </summary>
    public enum AccountType
    {
        Saving, Current, FixedDeposit
    }
}

